#version 330

in vec2 texCoord;
uniform sampler2D sampler;
uniform vec3 tint = vec3(1.0, 1.0, 1.0);

void main()
{
	vec4 texColor = texture2D(sampler, texCoord.xy);
	gl_FragColor = vec4(tint, 1);
}